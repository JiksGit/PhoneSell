# ─── Nginx Ingress (현재 비활성화) ───────────────────────────────────────────
# 사용하려면 아래 주석을 해제하고 host 값을 실제 도메인으로 수정하세요.
# 전제 조건: kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.10.0/deploy/static/provider/cloud/deploy.yaml
#
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: sungji-phone-ingress
#   namespace: sungji-phone
#   labels:
#     app: sungji-phone
#     component: ingress
#   annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /
#     nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
#     nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
# spec:
#   ingressClassName: nginx
#   rules:
#     - host: sungji-phone.example.com       # 실제 도메인으로 변경
#       http:
#         paths:
#           - path: /api
#             pathType: Prefix
#             backend:
#               service:
#                 name: sungji-backend
#                 port:
#                   number: 8080
#           - path: /grafana
#             pathType: Prefix
#             backend:
#               service:
#                 name: grafana
#                 port:
#                   number: 3000
#   # TLS 설정 (cert-manager 사용 시)
#   # tls:
#   #   - hosts:
#   #       - sungji-phone.example.com
#   #     secretName: sungji-phone-tls
